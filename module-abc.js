
import {get,brl,esc,skeleton,errorBox} from './module-common.js';import {calc} from './worker-client-v7.js';
export async function mount(root){root.innerHTML=`<div class="card"><div class="grid"><div class="field"><label>De</label><input id="f" type="date"></div><div class="field"><label>Até</label><input id="t" type="date"></div><div class="field"><label>Unidade</label><input id="u" placeholder="OKEO Consolidado" value="OKEO Consolidado"></div><div class="field"><label>&nbsp;</label><button id="go" class="primary">Atualizar</button></div></div></div><div id="out"></div>`;async function run(){const o=root.querySelector('#out');o.innerHTML=skeleton();try{const r=await get('analytics_abc',{from:root.querySelector('#f').value,to:root.querySelector('#t').value,unit:root.querySelector('#u').value},{ttl:300000});const a=await calc('abc',{rows:r.rows||[]});const counts={A:0,B:0,C:0},vals={A:0,B:0,C:0};a.rows.forEach(x=>{counts[x.abc]++;vals[x.abc]+=x.revenue});o.innerHTML=`<div class="kpis">${['A','B','C'].map(c=>`<div class="kpi"><small>Curva ${c}</small><b>${counts[c]} produtos</b><small>${brl(vals[c])}</small></div>`).join('')}</div><div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>Curva</th><th>Produto</th><th>Qtd.</th><th>Faturamento</th><th>%</th></tr></thead><tbody>${a.rows.slice(0,300).map(x=>`<tr><td><span class="pill">${x.abc}</span></td><td>${esc(x.product)}</td><td>${x.qty}</td><td>${brl(x.revenue)}</td><td>${x.pct.toFixed(1)}%</td></tr>`).join('')}</tbody></table></div></div>`}catch(e){o.innerHTML=errorBox(e)}}root.querySelector('#go').onclick=run;run()}
