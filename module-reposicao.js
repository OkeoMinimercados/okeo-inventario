
import {get,esc,skeleton,errorBox} from './module-common.js';import {calc} from './worker-client-v7.js';
export async function mount(root){root.innerHTML=skeleton(8);try{const [w,s]=await Promise.all([get('analytics_weekly',{weeks:12,unit:'OKEO Consolidado'},{ttl:300000}),get('v7_stock_page',{page:1,pageSize:5000},{ttl:120000})]);const rows=await calc('replenishment',{weekly:w.rows||[],stock:s.rows||[]});root.innerHTML=`<div class="kpis">${[['Repor',rows.filter(x=>x.status==='REPOR').length],['Atenção',rows.filter(x=>x.status==='ATENCAO').length],['OK',rows.filter(x=>x.status==='OK').length],['Sem giro',rows.filter(x=>x.status==='SEM_GIRO').length]].map(x=>`<div class="kpi"><small>${x[0]}</small><b>${x[1]}</b></div>`).join('')}</div><div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>Status</th><th>Unidade</th><th>Produto</th><th>Média/sem.</th><th>Pico</th><th>Atual</th><th>Ideal</th><th>Repor</th></tr></thead><tbody>${rows.slice(0,500).map(x=>`<tr><td><span class="pill ${x.status==='REPOR'?'red':x.status==='ATENCAO'?'yellow':'green'}">${x.status}</span></td><td>${esc(x.unit)}</td><td>${esc(x.product)}</td><td>${x.avgWeekly.toFixed(1)}</td><td>${x.peak.toFixed(0)}</td><td>${x.current}</td><td>${x.ideal}</td><td><b>${x.suggested}</b></td></tr>`).join('')}</tbody></table></div></div>`}catch(e){root.innerHTML=errorBox(e)}}
